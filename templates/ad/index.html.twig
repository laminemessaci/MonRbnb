{% extends 'base.html.twig' %}


{% block title %}Nos annonces {% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4 justify-content-center"> Les annonces de nos hôtes</h1>

        <div class="row">
            {% for ad in pagination.data %}
                {% include'/ad/_ad.html.twig' %}
            {% endfor %}
        </div>
        {{ pagination.display() }}
    </div>

{% endblock %}
